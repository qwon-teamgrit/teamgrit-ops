const crypto=require('crypto');
const WORK_DOC_ID=process.env.TEAMGRIT_WORK_DOC_ID||'1Z5pJR4v7l9XzgVjALTcRPskkEYJoc_Ii5l6zA3gXqLI';
const SYSTEM_DEV_DOC_ID=process.env.SYSTEM_DEV_DOC_ID||'1TirSjrD8b2p07tHt5Gn9sOrQoJPigyFmwcgSbAXPSxc';
const MODELS=[process.env.PROJECT_SYNC_GEMINI_MODEL||'gemini-3.5-flash-lite','gemini-3.6-flash'];
const MAX_SOURCES=Math.max(100,Number(process.env.SOURCE_MAX_FILES||400));
const MAX_DEPTH=Math.max(4,Number(process.env.SOURCE_MAX_DEPTH||8));
const MAX_FILE_CHARS=Math.max(500000,Number(process.env.SOURCE_MAX_FILE_CHARS||2000000));
const BATCH_CHARS=Math.max(180000,Number(process.env.SOURCE_AI_BATCH_CHARS||420000));
const FOLDER='application/vnd.google-apps.folder';
const READABLE=new Set(['application/vnd.google-apps.document','application/vnd.google-apps.spreadsheet','application/vnd.google-apps.presentation','application/vnd.google-apps.form']);
const clean=v=>v==null?'':String(v).trim();
const arr=v=>Array.isArray(v)?v:[];
const safe=(v,n=1000)=>clean(v).slice(0,n);
const hash=v=>crypto.createHash('sha1').update(String(v)).digest('hex').slice(0,14);
const stableId=(p,v)=>`${p}_${hash(v)}`;
const cleanJSON=t=>String(t||'').replace(/^```json\s*/i,'').replace(/^```/,'').replace(/```$/,'').trim();
async function gf(token,url,opts={}){const r=await fetch(url,{...opts,headers:{Authorization:`Bearer ${token}`,...(opts.headers||{})}});if(!r.ok)throw new Error(`Google API ${r.status}: ${(await r.text()).slice(0,500)}`);if(r.status===204)return null;return (r.headers.get('content-type')||'').includes('json')?r.json():r.text()}
function driveId(url=''){for(const p of [/\/folders\/([A-Za-z0-9_-]+)/,/\/document\/d\/([A-Za-z0-9_-]+)/,/\/spreadsheets\/d\/([A-Za-z0-9_-]+)/,/\/presentation\/d\/([A-Za-z0-9_-]+)/,/\/forms\/d\/([A-Za-z0-9_-]+)/,/\/file\/d\/([A-Za-z0-9_-]+)/,/[?&]id=([A-Za-z0-9_-]+)/]){const m=String(url).match(p);if(m)return m[1]}return ''}
function textLinks(text=''){return [...new Set((String(text).match(/https?:\/\/[^\s<>()"']+/g)||[]).map(x=>x.replace(/[),.;]+$/,'')))]}
function sourceUrl(id,mime=''){if(mime==='application/vnd.google-apps.document')return `https://docs.google.com/document/d/${id}/edit`;if(mime==='application/vnd.google-apps.spreadsheet')return `https://docs.google.com/spreadsheets/d/${id}/edit`;if(mime==='application/vnd.google-apps.presentation')return `https://docs.google.com/presentation/d/${id}/edit`;if(mime==='application/vnd.google-apps.form')return `https://docs.google.com/forms/d/${id}/edit`;if(mime===FOLDER)return `https://drive.google.com/drive/folders/${id}`;return `https://drive.google.com/open?id=${id}`}
function docData(doc){const out=[],links=[];let chars=0,truncated=false;function walk(content=[]){for(const b of content){for(const e of b.paragraph?.elements||[]){const t=e.textRun?.content||'';if(t){out.push(t);chars+=t.length}const u=e.textRun?.textStyle?.link?.url;if(u)links.push(u);if(chars>=MAX_FILE_CHARS){truncated=true;return}}for(const row of b.table?.tableRows||[])for(const cell of row.tableCells||[]){walk(cell.content||[]);if(truncated)return}}}walk(doc.body?.content||[]);for(const tab of doc.tabs||[]){if(truncated)break;walk(tab.documentTab?.body?.content||[])}const text=out.join('').slice(0,MAX_FILE_CHARS);return {text,links:[...new Set([...links,...textLinks(text)])],truncated}}
async function sheetData(token,id){const meta=await gf(token,`https://sheets.googleapis.com/v4/spreadsheets/${id}?fields=sheets.properties(title)`),lines=[],links=[];let chars=0,truncated=false;for(const sh of meta.sheets||[]){if(truncated)break;const title=sh.properties?.title||'Sheet';let d;try{d=await gf(token,`https://sheets.googleapis.com/v4/spreadsheets/${id}/values/${encodeURIComponent(`'${title.replace(/'/g,"''")}'`)}?majorDimension=ROWS`)}catch{continue}const head=`# ${title}\n`;lines.push(head);chars+=head.length;for(const row of d.values||[]){const line=row.map(v=>String(v??'')).join(' | ')+'\n';if(clean(line)){lines.push(line);chars+=line.length;links.push(...textLinks(line))}if(chars>=MAX_FILE_CHARS){truncated=true;break}}}return {text:lines.join('').slice(0,MAX_FILE_CHARS),links:[...new Set(links)],truncated}}
function slideData(p){const out=[],links=[];let chars=0,truncated=false;for(const [i,s] of (p.slides||[]).entries()){const h=`# Slide ${i+1}\n`;out.push(h);chars+=h.length;for(const pe of s.pageElements||[]){for(const el of pe.shape?.text?.textElements||[]){const t=el.textRun?.content||'';if(t){out.push(t);chars+=t.length}const u=el.textRun?.style?.link?.url;if(u)links.push(u);links.push(...textLinks(t));if(chars>=MAX_FILE_CHARS){truncated=true;break}}if(truncated)break}if(truncated)break}return {text:out.join('').slice(0,MAX_FILE_CHARS),links:[...new Set(links)],truncated}}
function formData(f){const out=[f.info?.title||'Form'],links=[];let chars=out[0].length,truncated=false;for(const [i,it] of (f.items||[]).entries()){const t=`\n${i+1}. ${it.title||''}`;out.push(t);chars+=t.length;links.push(...textLinks(t));if(chars>=MAX_FILE_CHARS){truncated=true;break}}return {text:out.join('').slice(0,MAX_FILE_CHARS),links:[...new Set(links)],truncated}}
async function meta(token,id){return gf(token,`https://www.googleapis.com/drive/v3/files/${encodeURIComponent(id)}?fields=id,name,mimeType,modifiedTime,webViewLink,trashed`)}
async function listFolder(token,id){let tokenPage='',out=[];do{const q=encodeURIComponent(`'${id}' in parents and trashed=false`);const d=await gf(token,`https://www.googleapis.com/drive/v3/files?q=${q}&pageSize=100&fields=nextPageToken,files(id,name,mimeType,modifiedTime,webViewLink,trashed)${tokenPage?`&pageToken=${encodeURIComponent(tokenPage)}`:''}`);out.push(...(d.files||[]));tokenPage=d.nextPageToken||''}while(tokenPage&&out.length<MAX_SOURCES);return out}
async function readFile(token,m){if(m.mimeType==='application/vnd.google-apps.document')return docData(await gf(token,`https://docs.googleapis.com/v1/documents/${m.id}?includeTabsContent=true`));if(m.mimeType==='application/vnd.google-apps.spreadsheet')return sheetData(token,m.id);if(m.mimeType==='application/vnd.google-apps.presentation')return slideData(await gf(token,`https://slides.googleapis.com/v1/presentations/${m.id}`));if(m.mimeType==='application/vnd.google-apps.form')return formData(await gf(token,`https://forms.googleapis.com/v1/forms/${m.id}`));return {text:'',links:[],truncated:false}}
async function collectCorpus(token){const roots=[{id:WORK_DOC_ID,root:'2026년 팀그릿 업무진행'},{id:SYSTEM_DEV_DOC_ID,root:'시스템개발팀'}],queue=roots.map(x=>({...x,parentId:'',depth:0})),seen=new Set(),sources=[],texts=[];let globalPartial=false;while(queue.length){if(sources.length>=MAX_SOURCES){globalPartial=true;break}const item=queue.shift();if(!item?.id||seen.has(item.id))continue;seen.add(item.id);let m;try{m=await meta(token,item.id)}catch(e){sources.push({sourceId:stableId('source',item.id),fileId:item.id,title:item.root||item.id,mimeType:'',url:sourceUrl(item.id),parentSourceId:item.parentId?stableId('source',item.parentId):'',rootSource:item.root,status:'연결 안 됨',readStatus:'연결 안 됨',readDetail:e.message,contentChars:0,discoveredLinks:0,modifiedTime:'',error:e.message});continue}const base={sourceId:stableId('source',m.id),fileId:m.id,title:m.name||item.root||m.id,mimeType:m.mimeType||'',url:m.webViewLink||sourceUrl(m.id,m.mimeType),parentSourceId:item.parentId?stableId('source',item.parentId):'',rootSource:item.root,status:'연결됨',readStatus:'전체 읽음',readDetail:'',contentChars:0,discoveredLinks:0,modifiedTime:m.modifiedTime||'',error:''};if(m.mimeType===FOLDER){try{const children=await listFolder(token,m.id);base.discoveredLinks=children.length;if(item.depth>=MAX_DEPTH&&children.length){base.readStatus='일부 읽음';base.readDetail='링크 탐색 깊이 한도';globalPartial=true}else for(const c of children){if(queue.length+sources.length>=MAX_SOURCES){base.readStatus='일부 읽음';base.readDetail='원본 수집 개수 한도';globalPartial=true;break}if(!seen.has(c.id))queue.push({id:c.id,root:item.root,parentId:m.id,depth:item.depth+1})}}catch(e){base.status='연결 안 됨';base.readStatus='연결 안 됨';base.readDetail=e.message;base.error=e.message}sources.push(base);continue}if(!READABLE.has(m.mimeType)){base.status='연결 안 됨';base.readStatus='연결 안 됨';base.readDetail='현재 자동 본문 분석 미지원 파일 형식';base.error=base.readDetail;sources.push(base);continue}try{const r=await readFile(token,m);base.contentChars=(r.text||'').length;base.discoveredLinks=(r.links||[]).length;if(r.truncated){base.readStatus='일부 읽음';base.readDetail=`파일 본문 ${MAX_FILE_CHARS.toLocaleString()}자 처리 한도`;globalPartial=true}texts.push({sourceId:base.sourceId,title:base.title,rootSource:base.rootSource,url:base.url,text:r.text||''});if(item.depth>=MAX_DEPTH&&(r.links||[]).some(u=>driveId(u))){base.readStatus='일부 읽음';base.readDetail=base.readDetail||'링크 탐색 깊이 한도';globalPartial=true}else for(const u of r.links||[]){const id=driveId(u);if(!id||seen.has(id))continue;if(queue.length+sources.length>=MAX_SOURCES){base.readStatus='일부 읽음';base.readDetail=base.readDetail||'원본 수집 개수 한도';globalPartial=true;break}queue.push({id,root:item.root,parentId:m.id,depth:item.depth+1})}}catch(e){base.status='연결 안 됨';base.readStatus='연결 안 됨';base.readDetail=e.message;base.error=e.message}sources.push(base)}const required=roots.map(r=>{const s=sources.find(x=>x.fileId===r.id);return {id:r.id,title:r.root,status:s?.status||'연결 안 됨',readStatus:s?.readStatus||'연결 안 됨',url:s?.url||sourceUrl(r.id,'application/vnd.google-apps.document'),error:s?.error||''}});return {sources,texts,required,partial:globalPartial}}
async function gemini(prompt){if(!process.env.GEMINI_API_KEY)throw new Error('GEMINI_API_KEY 미설정');const errors=[];for(const m of [...new Set(MODELS)]){try{const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${encodeURIComponent(m)}:generateContent?key=${encodeURIComponent(process.env.GEMINI_API_KEY)}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({contents:[{role:'user',parts:[{text:prompt}]}],generationConfig:{responseMimeType:'application/json',temperature:.1}})});if(!r.ok){errors.push(`${m}: ${r.status} ${(await r.text()).slice(0,220)}`);continue}const d=await r.json(),txt=(d.candidates?.[0]?.content?.parts||[]).map(p=>p.text||'').join('');return {data:JSON.parse(cleanJSON(txt)),modelUsed:m}}catch(e){errors.push(`${m}: ${e.message||e}`)}}throw new Error(`Gemini 호출 실패: ${errors.join(' | ')}`)}
function normName(v=''){return clean(v).toLowerCase().replace(/^\[[^\]]+\]\s*/,'').replace(/[\s_\-–—·:()\[\]]+/g,'').replace(/프로젝트$/,'')}
function normalize(p){return {name:safe(p.name,180),category:safe(p.category,80),status:safe(p.status,80),customer:safe(p.customer,220),summary:safe(p.summary,1500),evidence:safe(p.evidence,1500),sourceIds:[...new Set(arr(p.sourceIds).map(clean).filter(Boolean))],features:arr(p.features).map(f=>({name:safe(f.name||f,180),description:safe(f.description,700),status:safe(f.status,80)})).filter(f=>f.name),devices:arr(p.devices).map(d=>({name:safe(d.name||d,180),role:safe(d.role,500),model:safe(d.model,180)})).filter(d=>d.name)}}
function batches(texts){const result=[];let cur='';for(const s of texts){const prefix=`\n<SOURCE id="${s.sourceId}" title="${s.title}" root="${s.rootSource}" url="${s.url}">\n`,suffix='\n</SOURCE>';for(let i=0;i<s.text.length||i===0;i+=BATCH_CHARS){const piece=prefix+s.text.slice(i,i+BATCH_CHARS)+suffix;if(cur&&cur.length+piece.length>BATCH_CHARS){result.push(cur);cur=''}cur+=piece;if(!s.text.length)break}}if(cur)result.push(cur);return result}
async function extractProjects(corpus){const candidates=[];let modelUsed='';for(const batch of batches(corpus.texts)){const p=`TeamGRIT 내부 최신 원본 자료에서 실제 프로젝트를 추출한다. SOURCE에 있는 내용만 사용하고 추정하지 않는다. Project/PoC/납품/행사/제품 개편 등 독립 업무 단위만 project로 반환한다. 단순 Feature/Research/제품명은 독립 프로젝트로 만들지 않는다. 동일 프로젝트의 기능/장비/고객/상태/근거를 연결한다. 빈 정보는 빈 문자열로 둔다. sourceIds는 근거 SOURCE id만 넣는다. JSON만 반환: {"projects":[{"name":string,"category":string,"status":string,"customer":string,"summary":string,"evidence":string,"sourceIds":[string],"features":[{"name":string,"description":string,"status":string}],"devices":[{"name":string,"role":string,"model":string}]}]}.\n${batch}`;const ai=await gemini(p);modelUsed=ai.modelUsed;for(const x of arr(ai.data?.projects)){const n=normalize(x);if(n.name)candidates.push(n)}}const merged=new Map();for(const p of candidates){const k=normName(p.name);if(!k)continue;if(!merged.has(k)){merged.set(k,p);continue}const a=merged.get(k);a.sourceIds=[...new Set([...a.sourceIds,...p.sourceIds])];a.features=[...a.features,...p.features].filter((x,i,z)=>z.findIndex(y=>normName(y.name)===normName(x.name))===i);a.devices=[...a.devices,...p.devices].filter((x,i,z)=>z.findIndex(y=>normName(y.name)===normName(x.name))===i);for(const f of ['category','status','customer','summary','evidence'])if(!a[f]&&p[f])a[f]=p[f]}return {projects:[...merged.values()],modelUsed,truncated:corpus.partial}}
function buildEntities(projects,existing=[]){const oldById=new Map(existing.map(p=>[p.project_id||p.id,p])),oldByName=new Map(existing.map(p=>[normName(p.name),p]));const now=new Date().toISOString(),out=[],features=[],devices=[];for(const p of projects){const key=normName(p.name),id=stableId('project',key||p.name),old=oldById.get(id)||oldByName.get(key)||{},sourceIds=[...new Set(p.sourceIds||[])];out.push({project_id:id,name:p.name,source_category:p.category||'',source_status:p.status||'',customer:p.customer||'',summary:p.summary||'',drive_url:old.drive_url||old.driveUrl||'',figma_url:old.figma_url||old.figmaUrl||'',source_ids:sourceIds.join(','),source_evidence:p.evidence||'',last_synced_at:now,connection_status:'연결됨',manual_category:old.manual_category||'',manual_status:old.manual_status||'',manual_updated_at:old.manual_updated_at||''});for(const f of p.features||[])features.push({feature_id:stableId('feature',`${id}:${normName(f.name)}`),project_id:id,name:f.name,description:f.description||'',status:f.status||'',source_ids:sourceIds.join(','),last_synced_at:now});for(const d of p.devices||[])devices.push({device_id:stableId('device',`${id}:${normName(d.name)}`),project_id:id,name:d.name,role:d.role||'',model:d.model||'',source_ids:sourceIds.join(','),last_synced_at:now})}return {projects:out,features,devices}}
function sourceRows(corpus){const now=new Date().toISOString();return corpus.sources.map(s=>({source_id:s.sourceId,file_id:s.fileId,title:s.title,mime_type:s.mimeType,url:s.url,parent_source_id:s.parentSourceId,root_source:s.rootSource,status:s.status,read_status:s.readStatus,read_detail:s.readDetail,content_chars:s.contentChars,discovered_links:s.discoveredLinks,modified_time:s.modifiedTime,last_synced_at:now,error:s.error||''}))}
function contextText(corpus){return corpus.texts.map(s=>`\n<SOURCE id="${s.sourceId}" title="${s.title}" root="${s.rootSource}" url="${s.url}">\n${s.text}\n</SOURCE>`).join('')}
module.exports={WORK_DOC_ID,SYSTEM_DEV_DOC_ID,collectCorpus,extractProjects,buildEntities,sourceRows,contextText,gemini,clean,arr,stableId};
